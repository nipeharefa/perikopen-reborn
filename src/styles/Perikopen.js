import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
  namaTanggal: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 10
  }
})